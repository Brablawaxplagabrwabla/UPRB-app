import React, { Component } from 'react';
import { Image } from 'react-native';
import  { Container, Content, Grid, Row, Form, Input, Button, Text,  Item, Label }  from 'native-base';

export default Clases Loading extends Component {
  render() {
    return (
      <Container>
        <Content>
          <Text>Sirve</Text>
        </Content>
      </Container>
    );
  }
}
